account_id: ""
actions:
  node-extensions/node-extensions-library:
    runtime: v2
    sources: actions
    specs:
      chainlinkPriceFeed:
        description: Gets the data from the latest round on Chainlink price feed contract given the token pair.
        function: extension:chainlinkPriceFeed
        trigger:
          type: webhook
          webhook:
            authenticated: false
project_slug: ""
