account_id: ""
actions:
  node-extensions/node-extensions-library:
    runtime: v2
    sources: actions
    specs:
      simulate-mempool-transaction:
        description: This endpoint simulates a transaction with provided hash that is in the mempool.
        function: extension:simulateMempoolTransaction
        trigger:
          type: webhook
          webhook:
            authenticated: false

project_slug: ""
