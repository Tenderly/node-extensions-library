account_id: ""
actions:
  node-extension/node-extensions-library:
    runtime: v2
    sources: actions
    specs:
      sendSimulateRawTansaction:
        description: Simulates transaction and sends transaction on-chain if successful
        function: extension:sendRawTransaction
        trigger:
          type: webhook
          webhook:
            authenticated: false
project_slug: ""
