account_id: ""
actions:
  tenderly/extensions-library:
    runtime: v2
    sources: actions
    specs:
      sendSimulateRawTransaction:
        description: Simulates transaction and sends transaction on-chain if successful
        function: extension:sendSimulateRawTransaction
        trigger:
          type: webhook
          webhook:
            authenticated: false
project_slug: ""
