account_id: ""
actions:
  node-extensions/node-extensions-library:
    runtime: v2
    sources: actions
    specs:
      simulateSendRawTransaction:
        description: Simulates transaction and sends transaction on-chain if successful
        function: extension:sendRawTransaction
        trigger:
          type: webhook
          webhook:
            authenticated: false
project_slug: ""
